# purl2repo v2.0.1

`purl2repo` 2.0.1 improves resolver correctness against the official
`package-url/purl-spec` examples and adds safer fallback behavior for cases
where first-party metadata is missing, stale, or incomplete.

## Highlights

- Added parser conformance coverage using the official upstream purl-spec
  fixtures for every supported PURL type.
- Added a non-gating `scripts/purl_spec_resolution_report.py` helper to compare
  upstream examples against live repository validation, release-link
  verification, deps.dev fallback, and bounded scraping.
- Added deps.dev as a third-party fallback for PyPI, npm, Cargo, Maven, NuGet,
  and Go when native ecosystem metadata does not yield a usable repository.
- Improved Go support with `go-import` metadata for vanity imports and
  repository inference when the Go proxy cannot resolve commit-style versions.
- Improved PyPI behavior by falling back from missing version-specific JSON to
  project-level metadata for repository discovery.
- Improved generic PURL handling by normalizing `vcs_url` values with embedded
  revision suffixes back to the repository URL before validation.
- Added commit-style release-link inference for GitHub, GitLab, and Bitbucket.
- Tightened fallback scraping so generic navigation, support, policy, and
  analysis pages are not promoted to repository candidates.

## Notes

- deps.dev is intentionally lower-confidence third-party evidence. It is used
  after native metadata and before HTML scraping, and returned URLs are still
  validated before becoming canonical.
- Several upstream purl-spec examples are parser/canonicalization examples, not
  live public packages. The report script is intentionally informational and
  does not fail builds for stale, synthetic, or private examples.

## Artifacts

- `purl2repo-2.0.1.tar.gz`
- `purl2repo-2.0.1-py3-none-any.whl`
